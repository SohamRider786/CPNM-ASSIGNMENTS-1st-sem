#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    FILE *ptr4=NULL;
    ptr4=fopen(argv[2],"a+");
    while(!feof(ptr4))
    {
        printf("ggwp\n");
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    fclose(ptr2);
    fclose(ptr3);
    fclose(ptr1);
    return 0;
} 
ÿ#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            break;  
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[2],"r");
    while(!feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    fclose(ptr3);
    fclose(ptr1);
    return 0;
} 
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            break;  
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[2],"r");
    while(!feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
} 
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                fprintf(ptr2,"%c",str2);
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[2],"r");
    while(!feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
        if(str==EOF)
        break;
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
} 
ÿ#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                fprintf(ptr2,"%c",str2);
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[2],"r");
    while(!feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
        if(str==EOF)
        break;
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
} 
ÿ#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                fprintf(ptr2,"%c",str2);
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[2],"r");
    while(getc(ptr4)!=EOF)
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
        if(str==EOF)
        break;
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
} 
ÿ#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                fprintf(ptr2,"%c",str2);
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    return 0;
}ÿ#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                fprintf(ptr2,"%c",str2);
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen("second.txt","a+");
    rewind(ptr4);
    while(!feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
}ÿ#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r+");
    rewind(ptr4);
    while(!feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r+");
    rewind(ptr4);
    while(!feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                if(str4==' '||str4==';')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r+");
    rewind(ptr4);
    while(feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                if(str4==' '||str4==';')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r+");
    rewind(ptr4);
    while(!feof(ptr4))
    {
        str4=getc(ptr4);
        if(str4=='\n')
        {
            str4=getc(ptr4);
            while((str4!=' ')||(str4!=';'))
            {
                if(str4==' '||str4==';')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr4);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr4);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr4);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    fclose(ptr3);
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr3);
    while(!feof(ptr3))
    {
        str4=getc(ptr3);
        if(str4=='\n')
        {
            str4=getc(ptr3);
            while((str4!=' ')||(str4!=';'))
            {
                if(str4==' '||str4==';')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr3);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr3);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr3);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr3);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr3);
    while(!feof(ptr3))
    {
        str4=getc(ptr3);
        if(str4=='\n')
        {
            str4=getc(ptr3);
            while((str4!=' ')||(str4!=';'))
            {
                if(str4==' '||str4==';')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr3);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr3);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr3);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr3);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[20];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    fclose(ptr1);
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            k=k+2;
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr3);
    while(!feof(ptr3))
    {
        str4=getc(ptr3);
        if(str4=='\n')
        {
            str4=(char)getc(ptr3);
            while((str4!=' ')||(str4!=';'))
            {
                if(str4==' '||str4==';')
                break;
                str_data_type[j++]=str4;
                str4=(char)getc(ptr3);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr3);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr3);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr3);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[300];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
        str4=getc(ptr1);
        if(str4=='\n')
        {
            str4=(char)getc(ptr1);
            while((str4!=' ')||(str4!=';'))
            {
                if(str4==' '||str4==';')
                break;
                str_data_type[j++]=str4;
                str4=(char)getc(ptr1);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr1);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr1);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[300];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
        str4=getc(ptr1);
        if(str4=='\n')
        {
            str4=getc(ptr1);
            while(str4!=';')
            {
                if(str4==' '||str4==';'||str4=='{'||str4=='}')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr1);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr1);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr1);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[300];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
        str4=getc(ptr1);
        if(str4=='\n')
        {
            str4=getc(ptr1);
            while(str4!=';')
            {
                if(str4==' '||str4==';'||str4=='{'||str4=='}'||str4=='\n')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr1);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr1);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr1);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[300];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
        str4=getc(ptr1);
        if(str4=='\n')
        {
            str4=getc(ptr1);
            while(str4!=';'||str4!='\n')
            {
                if(str4==' ')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr1);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr1);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr1);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
        str4=getc(ptr1);
        if(str4=='\n')
        {
            str4=getc(ptr1);
            while(1)
            {
                if(str4==' '||str4==';'||str4=='\n')
                break;
                str_data_type[j++]=str4;
                str4=getc(ptr1);
            }
            str_data_type[j]='\0';
            printf("\n\n%s\n\n",str_data_type);
            if((strcmp(str_data_type,"int")==0)||(strcmp(str_data_type,"float")==0)||(strcmp(str_data_type,"double")==0)||(strcmp(str_data_type,"char")==0)||(strcmp(str_data_type,"const char")==0))
            {
                c++;
                j=0;
                str4=getc(ptr1);
                while(str4!='\n')
                {
                    if(str4==',')
                    {
                        c++;
                    }
                    str4=getc(ptr1);
                }
            }
        }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=getc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str4=getc(ptr1);
               if(str4==' '||str4=='\n')
               break;
               str_data_type[j++]=str4;
           }
           str_data_type[j]='\0';
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str4=getc(ptr1);
                   if(str4==',')
                   c++;
                   if(str4==';')
                   break;
               }
           }
       }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=getc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str4=getc(ptr1);
               if(str4==' '||str4=='\n')
               break;
               str_data_type[j++]=str4;
           }
           str_data_type[j]='\0';
           puts(str_data_type);
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str4=getc(ptr1);
                   if(str4==',')
                   c++;
                   if(str4==';')
                   break;
               }
           }
       }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4,str3,str5;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=getc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str3=getc(ptr1);
               if(str3==' '||str3=='\n')
               break;
               str_data_type[j++]=str3;
           }
           str_data_type[j]='\0';
           puts(str_data_type);
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str5=getc(ptr1);
                   if(str5==',')
                   c++;
                   if(str5==';')
                   break;
               }
           }
       }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4,str3,str5;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=getc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str3=getc(ptr1);
               if(str3==' '||str3=='\n')
               break;
               str_data_type[j++]=str3;
           }
           str_data_type[j]='\0';
           puts(str_data_type);
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str5=getc(ptr1);
                   if(str5==',')
                   c++;
                   if(str5==';')
                   break;
               }
           }
       }
       j=0;
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4,str3,str5;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=getc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str3=getc(ptr1);
               if(str3==' '||str3=='\n')
               break;
               str_data_type[j++]=str3;
           }
           str_data_type[j]='\0';
           puts(str_data_type);
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str5=getc(ptr1);
                   if(str5==',')
                   c++;
                   if(str5==';')
                   break;
               }
           }
           j=0;
       }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4,str3,str5;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=getc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str4=getc(ptr1);
               if(str4==' '||str4=='\n')
               break;
               str_data_type[j++]=str4;
           }
           str_data_type[j]='\0';
           puts(str_data_type);
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str4=getc(ptr1);
                   if(str4==',')
                   c++;
                   if(str4==';')
                   break;
               }
           }
           j=0;
       }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4,str3,str5;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=fgetc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str4=fgetc(ptr1);
               if(str4==' '||str4=='\n')
               break;
               str_data_type[j++]=str4;
           }
           str_data_type[j]='\0';
           puts(str_data_type);
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str4=fgetc(ptr1);
                   if(str4==',')
                   c++;
                   if(str4==';')
                   break;
               }
           }
           j=0;
       }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4,str3,str5;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=fgetc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str4=fgetc(ptr1);
               if(str4==' '||str4=='\n')
               break;
               str_data_type[j++]=str4;
           }
           str_data_type[j]='\0';
           puts(str_data_type);
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str4=fgetc(ptr1);
                   if(str4==',')
                   c++;
                   if(str4==';')
                   break;
               }
           }
           j=0;
       }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    char str_data_type[30];
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str4,str3,str5;
    int i=0,k=0,c=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    while(!feof(ptr1))
    {
       str4=fgetc(ptr1);
       if(str4=='\n')
       {
           while(1)
           {
               str4=fgetc(ptr1);
               if(str4==' '||str4=='\n')
               break;
               str_data_type[j++]=str4;
           }
           str_data_type[j]='\0';
           puts(str_data_type);
           if(strcmp(str_data_type,"int")==0||strcmp(str_data_type,"float")==0||strcmp(str_data_type,"char")==0)
           {
               c++;
               while(1)
               {
                   str4=fgetc(ptr1);
                   if(str4==',')
                   c++;
                   if(str4==';')
                   break;
               }
           }
           for(int k=0;k<strlen(str_data_type);k++)
           str_data_type[i]='\0';
           j=0;
       }
    }
    printf("the total number of variables in the program of each built in data types=%d",c);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    rewind(ptr1);
    char str_data_type[30]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr1);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr1);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char") == 0) 
            f=1;
            else if(f&&str3=='\n')
            f=0;
            for(int i=0;i<strlen(str_data_type)+1;i++)
            {
                str_data_type[i]='\0';
            }
            counter=-1;
            str3=fgetc(ptr1);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr1);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r");
    char str_data_type[30]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr1);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr1);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char") == 0) 
            f=1;
            else if(f&&str3=='\n')
            f=0;
            for(int i=0;i<strlen(str_data_type)+1;i++)
            {
                str_data_type[i]='\0';
            }
            counter=-1;
            str3=fgetc(ptr1);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r");
    char str_data_type[30]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr1);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr1);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char") == 0) 
            f=1;
            else if(f&&str3=='\n')
            f=0;
            for(int i=0;i<strlen(str_data_type)+1;i++)
            {
                str_data_type[i]='\0';
            }
            counter=-1;
            str3=fgetc(ptr1);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r");
    char str_data_type[30]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char") == 0) 
            f=1;
            else if(f&&str3=='\n')
            f=0;
            for(int i=0;i<strlen(str_data_type)+1;i++)
            {
                str_data_type[i]='\0';
            }
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char") == 0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            for(int i=0;i<strlen(str_data_type)+1;i++)
            {
                str_data_type[i]='\0';
            }
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"r");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char") == 0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int i=0;i<n+1;i++)
            str_data_type[i]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char") == 0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int i=0;i<n+1;i++)
            str_data_type[i]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int i=0;i<n+1;i++)
            str_data_type[i]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int i=0;i<n+1;i++)
            str_data_type[i]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int i=0;i<n+1;i++)
            str_data_type[i]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0||strcmp(str_data_type,"FILE")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int i=0;i<n+1;i++)
            str_data_type[i]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int i=0;i<n+1;i++)
            str_data_type[i]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int j=0;j<n+1;j++)
            str_data_type[j]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter+3);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0||strcmp(str_data_type,"const char")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int j=0;j<n+1;j++)
            str_data_type[j]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter+3);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int j=0;j<n+1;j++)
            str_data_type[j]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int j=0;j<n+1;j++)
            str_data_type[j]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types except commoand line arguments and file pointers=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int j=0;j<n+1;j++)
            str_data_type[j]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types except commoand line arguments and file pointers=%d",var_counter);
    fclose(ptr4);
    return 0;
}#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc,const char *argv[])
{
    FILE *ptr1=NULL;
    FILE *ptr2=NULL;
    char str,str2,str3;
    int i=0,k=0,j=0;
    double percent;
    ptr1=fopen(argv[1],"a+");
    while(!feof(ptr1))
    {
        str=fgetc(ptr1);
        if(str!=' ')
        i++;
        printf("%c",str);
    }
    FILE *ptr3=NULL;
    ptr3=fopen(argv[1],"a+");
    ptr2=fopen(argv[2],"a+");
    while(!feof(ptr3))
    {
        str2=fgetc(ptr3);
        if(str2=='/&&(fgetc(ptr3)=='*'))
        {
            while(!feof(ptr3))
            {
                str2=fgetc(ptr3);
                if(str2!=' ')
                k++;
                if(str2=='/)
                break;
            }
        }
        else
        {
            if(str2==EOF)
            {
                break;  
            }
            fprintf(ptr2,"%c",str2);
        }
    }
    percent=(double)k/double)i*100;
    printf("the percentage of characters in the comments is=%0.3lf",percent);
    printf("\n");
    fclose(ptr2);
    fclose(ptr1);
    FILE *ptr4=NULL;
    ptr4=fopen(argv[1],"a+");
    char str_data_type[100]="";
    unsigned counter=0,var_counter=0,f=0;
    str3=fgetc(ptr4);
    while(str3!=EOF)
    {
        str_data_type[counter]=str3;
        str3=fgetc(ptr4);
        if(str3==' '||str3=='\n')
        {
            if(strcmp(str_data_type, "int") == 0 || strcmp(str_data_type, "float") == 0 || strcmp(str_data_type, "char")==0||strcmp(str_data_type,"double")==0||strcmp(str_data_type,"unsigned")==0) 
            f=1;
            else if(f&&(str3=='\n'))
            f=0;
            int n=strlen(str_data_type);
            for(int j=0;j<n+1;j++)
            str_data_type[j]='\0';
            counter=-1;
            str3=fgetc(ptr4);
        }
        if(f&&(str3==','||str3==';'))
        var_counter++;
        counter++;
    }
    printf("the total number of variables in the program of each built in data types except commoand line arguments and file pointers=%d",var_counter);
    fclose(ptr4);
    return 0;
}